[project]
name = "newcomer-practice"
version = "0.1.0"
dependencies = ["biopython"]

[project.scripts]
kadaie_1 = "newcomer_practice.main:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
